interface:
  display_name: "甲状腺项目导航"
  short_description: "按任务加载甲状腺项目事实、环境、发布和 QA 规则"
  default_prompt: "使用 $thyroid-project 先读取项目 AGENTS，再按任务类型只加载需要的 docs、代码和 references；默认使用本地 thyroid_local，生产与微信发布动作按独立授权边界执行。"
